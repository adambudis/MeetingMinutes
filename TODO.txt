TODO
====

[ ] GPU support in Docker
    - Add NVIDIA runtime to docker-compose.yml for the `api` service:

        deploy:
          resources:
            reservations:
              devices:
                - driver: nvidia
                  count: 1
                  capabilities: [gpu]

    - Switch Python base image in Dockerfile to a CUDA-enabled PyTorch image, e.g.:
        FROM pytorch/pytorch:2.5.1-cuda12.4-cudnn9-runtime AS runtime
      (then install .NET runtime on top instead of Python)

    - Requires NVIDIA Container Toolkit on the Docker host:
        https://docs.nvidia.com/datacenter/cloud-native/container-toolkit/install-guide.html
